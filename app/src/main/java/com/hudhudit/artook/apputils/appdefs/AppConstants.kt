package com.hudhudit.artook.apputils.appdefs

class AppConstants {
    companion object {

        const val USER: String = "Users"
        const val REPORTUSER: String = "Report"
        const val MESSAGEMODEL = "messageModel"
        const val VIEW_RESEVER = 2
        const val VIEW_SENDER = 1
        const val SECOND_MILLIS: Int = 1000
        const val MINUTE_MILLIS: Int = 60 * SECOND_MILLIS
        const val HOUR_MILLIS: Int = 60 * MINUTE_MILLIS
        const val DAY_MILLIS: Int = 24 * HOUR_MILLIS
    }
}